rsvplist.settings:
  type: config_object
  label: 'RSVP List Content Type Setttings'
  mapping:
    allowed_types:
      type: sequence
      label: 'Content types RSVP Form can display on'
      sequence:
        type: string
        label: 'Content type'
